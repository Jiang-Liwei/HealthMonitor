// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"healthmonitor/ent/foodnutrientsrelationships"
	"healthmonitor/ent/nutrient"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// NutrientCreate is the builder for creating a Nutrient entity.
type NutrientCreate struct {
	config
	mutation *NutrientMutation
	hooks    []Hook
}

// SetName sets the "name" field.
func (nc *NutrientCreate) SetName(s string) *NutrientCreate {
	nc.mutation.SetName(s)
	return nc
}

// SetEffect sets the "effect" field.
func (nc *NutrientCreate) SetEffect(n nutrient.Effect) *NutrientCreate {
	nc.mutation.SetEffect(n)
	return nc
}

// SetID sets the "id" field.
func (nc *NutrientCreate) SetID(u uuid.UUID) *NutrientCreate {
	nc.mutation.SetID(u)
	return nc
}

// SetNillableID sets the "id" field if the given value is not nil.
func (nc *NutrientCreate) SetNillableID(u *uuid.UUID) *NutrientCreate {
	if u != nil {
		nc.SetID(*u)
	}
	return nc
}

// AddFoodIDs adds the "food" edge to the FoodNutrientsRelationships entity by IDs.
func (nc *NutrientCreate) AddFoodIDs(ids ...int) *NutrientCreate {
	nc.mutation.AddFoodIDs(ids...)
	return nc
}

// AddFood adds the "food" edges to the FoodNutrientsRelationships entity.
func (nc *NutrientCreate) AddFood(f ...*FoodNutrientsRelationships) *NutrientCreate {
	ids := make([]int, len(f))
	for i := range f {
		ids[i] = f[i].ID
	}
	return nc.AddFoodIDs(ids...)
}

// Mutation returns the NutrientMutation object of the builder.
func (nc *NutrientCreate) Mutation() *NutrientMutation {
	return nc.mutation
}

// Save creates the Nutrient in the database.
func (nc *NutrientCreate) Save(ctx context.Context) (*Nutrient, error) {
	nc.defaults()
	return withHooks(ctx, nc.sqlSave, nc.mutation, nc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (nc *NutrientCreate) SaveX(ctx context.Context) *Nutrient {
	v, err := nc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (nc *NutrientCreate) Exec(ctx context.Context) error {
	_, err := nc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (nc *NutrientCreate) ExecX(ctx context.Context) {
	if err := nc.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (nc *NutrientCreate) defaults() {
	if _, ok := nc.mutation.ID(); !ok {
		v := nutrient.DefaultID()
		nc.mutation.SetID(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (nc *NutrientCreate) check() error {
	if _, ok := nc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New(`ent: missing required field "Nutrient.name"`)}
	}
	if _, ok := nc.mutation.Effect(); !ok {
		return &ValidationError{Name: "effect", err: errors.New(`ent: missing required field "Nutrient.effect"`)}
	}
	if v, ok := nc.mutation.Effect(); ok {
		if err := nutrient.EffectValidator(v); err != nil {
			return &ValidationError{Name: "effect", err: fmt.Errorf(`ent: validator failed for field "Nutrient.effect": %w`, err)}
		}
	}
	return nil
}

func (nc *NutrientCreate) sqlSave(ctx context.Context) (*Nutrient, error) {
	if err := nc.check(); err != nil {
		return nil, err
	}
	_node, _spec := nc.createSpec()
	if err := sqlgraph.CreateNode(ctx, nc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != nil {
		if id, ok := _spec.ID.Value.(*uuid.UUID); ok {
			_node.ID = *id
		} else if err := _node.ID.Scan(_spec.ID.Value); err != nil {
			return nil, err
		}
	}
	nc.mutation.id = &_node.ID
	nc.mutation.done = true
	return _node, nil
}

func (nc *NutrientCreate) createSpec() (*Nutrient, *sqlgraph.CreateSpec) {
	var (
		_node = &Nutrient{config: nc.config}
		_spec = sqlgraph.NewCreateSpec(nutrient.Table, sqlgraph.NewFieldSpec(nutrient.FieldID, field.TypeUUID))
	)
	if id, ok := nc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = &id
	}
	if value, ok := nc.mutation.Name(); ok {
		_spec.SetField(nutrient.FieldName, field.TypeString, value)
		_node.Name = value
	}
	if value, ok := nc.mutation.Effect(); ok {
		_spec.SetField(nutrient.FieldEffect, field.TypeEnum, value)
		_node.Effect = value
	}
	if nodes := nc.mutation.FoodIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   nutrient.FoodTable,
			Columns: []string{nutrient.FoodColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(foodnutrientsrelationships.FieldID, field.TypeInt),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// NutrientCreateBulk is the builder for creating many Nutrient entities in bulk.
type NutrientCreateBulk struct {
	config
	err      error
	builders []*NutrientCreate
}

// Save creates the Nutrient entities in the database.
func (ncb *NutrientCreateBulk) Save(ctx context.Context) ([]*Nutrient, error) {
	if ncb.err != nil {
		return nil, ncb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(ncb.builders))
	nodes := make([]*Nutrient, len(ncb.builders))
	mutators := make([]Mutator, len(ncb.builders))
	for i := range ncb.builders {
		func(i int, root context.Context) {
			builder := ncb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*NutrientMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, ncb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, ncb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, ncb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (ncb *NutrientCreateBulk) SaveX(ctx context.Context) []*Nutrient {
	v, err := ncb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (ncb *NutrientCreateBulk) Exec(ctx context.Context) error {
	_, err := ncb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ncb *NutrientCreateBulk) ExecX(ctx context.Context) {
	if err := ncb.Exec(ctx); err != nil {
		panic(err)
	}
}
