// Code generated by ent, DO NOT EDIT.

package ent

import (
	"HealthMonitor/ent/foodingredients"
	"HealthMonitor/ent/ingredients"
	"HealthMonitor/ent/predicate"
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// IngredientsQuery is the builder for querying Ingredients entities.
type IngredientsQuery struct {
	config
	ctx        *QueryContext
	order      []ingredients.OrderOption
	inters     []Interceptor
	predicates []predicate.Ingredients
	withFood   *FoodIngredientsQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the IngredientsQuery builder.
func (iq *IngredientsQuery) Where(ps ...predicate.Ingredients) *IngredientsQuery {
	iq.predicates = append(iq.predicates, ps...)
	return iq
}

// Limit the number of records to be returned by this query.
func (iq *IngredientsQuery) Limit(limit int) *IngredientsQuery {
	iq.ctx.Limit = &limit
	return iq
}

// Offset to start from.
func (iq *IngredientsQuery) Offset(offset int) *IngredientsQuery {
	iq.ctx.Offset = &offset
	return iq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (iq *IngredientsQuery) Unique(unique bool) *IngredientsQuery {
	iq.ctx.Unique = &unique
	return iq
}

// Order specifies how the records should be ordered.
func (iq *IngredientsQuery) Order(o ...ingredients.OrderOption) *IngredientsQuery {
	iq.order = append(iq.order, o...)
	return iq
}

// QueryFood chains the current query on the "food" edge.
func (iq *IngredientsQuery) QueryFood() *FoodIngredientsQuery {
	query := (&FoodIngredientsClient{config: iq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := iq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := iq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(ingredients.Table, ingredients.FieldID, selector),
			sqlgraph.To(foodingredients.Table, foodingredients.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, ingredients.FoodTable, ingredients.FoodColumn),
		)
		fromU = sqlgraph.SetNeighbors(iq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Ingredients entity from the query.
// Returns a *NotFoundError when no Ingredients was found.
func (iq *IngredientsQuery) First(ctx context.Context) (*Ingredients, error) {
	nodes, err := iq.Limit(1).All(setContextOp(ctx, iq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{ingredients.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (iq *IngredientsQuery) FirstX(ctx context.Context) *Ingredients {
	node, err := iq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Ingredients ID from the query.
// Returns a *NotFoundError when no Ingredients ID was found.
func (iq *IngredientsQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = iq.Limit(1).IDs(setContextOp(ctx, iq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{ingredients.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (iq *IngredientsQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := iq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Ingredients entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Ingredients entity is found.
// Returns a *NotFoundError when no Ingredients entities are found.
func (iq *IngredientsQuery) Only(ctx context.Context) (*Ingredients, error) {
	nodes, err := iq.Limit(2).All(setContextOp(ctx, iq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{ingredients.Label}
	default:
		return nil, &NotSingularError{ingredients.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (iq *IngredientsQuery) OnlyX(ctx context.Context) *Ingredients {
	node, err := iq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Ingredients ID in the query.
// Returns a *NotSingularError when more than one Ingredients ID is found.
// Returns a *NotFoundError when no entities are found.
func (iq *IngredientsQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = iq.Limit(2).IDs(setContextOp(ctx, iq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{ingredients.Label}
	default:
		err = &NotSingularError{ingredients.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (iq *IngredientsQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := iq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of IngredientsSlice.
func (iq *IngredientsQuery) All(ctx context.Context) ([]*Ingredients, error) {
	ctx = setContextOp(ctx, iq.ctx, ent.OpQueryAll)
	if err := iq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Ingredients, *IngredientsQuery]()
	return withInterceptors[[]*Ingredients](ctx, iq, qr, iq.inters)
}

// AllX is like All, but panics if an error occurs.
func (iq *IngredientsQuery) AllX(ctx context.Context) []*Ingredients {
	nodes, err := iq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Ingredients IDs.
func (iq *IngredientsQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if iq.ctx.Unique == nil && iq.path != nil {
		iq.Unique(true)
	}
	ctx = setContextOp(ctx, iq.ctx, ent.OpQueryIDs)
	if err = iq.Select(ingredients.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (iq *IngredientsQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := iq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (iq *IngredientsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, iq.ctx, ent.OpQueryCount)
	if err := iq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, iq, querierCount[*IngredientsQuery](), iq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (iq *IngredientsQuery) CountX(ctx context.Context) int {
	count, err := iq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (iq *IngredientsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, iq.ctx, ent.OpQueryExist)
	switch _, err := iq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (iq *IngredientsQuery) ExistX(ctx context.Context) bool {
	exist, err := iq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the IngredientsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (iq *IngredientsQuery) Clone() *IngredientsQuery {
	if iq == nil {
		return nil
	}
	return &IngredientsQuery{
		config:     iq.config,
		ctx:        iq.ctx.Clone(),
		order:      append([]ingredients.OrderOption{}, iq.order...),
		inters:     append([]Interceptor{}, iq.inters...),
		predicates: append([]predicate.Ingredients{}, iq.predicates...),
		withFood:   iq.withFood.Clone(),
		// clone intermediate query.
		sql:  iq.sql.Clone(),
		path: iq.path,
	}
}

// WithFood tells the query-builder to eager-load the nodes that are connected to
// the "food" edge. The optional arguments are used to configure the query builder of the edge.
func (iq *IngredientsQuery) WithFood(opts ...func(*FoodIngredientsQuery)) *IngredientsQuery {
	query := (&FoodIngredientsClient{config: iq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	iq.withFood = query
	return iq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Ingredients.Query().
//		GroupBy(ingredients.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (iq *IngredientsQuery) GroupBy(field string, fields ...string) *IngredientsGroupBy {
	iq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &IngredientsGroupBy{build: iq}
	grbuild.flds = &iq.ctx.Fields
	grbuild.label = ingredients.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.Ingredients.Query().
//		Select(ingredients.FieldName).
//		Scan(ctx, &v)
func (iq *IngredientsQuery) Select(fields ...string) *IngredientsSelect {
	iq.ctx.Fields = append(iq.ctx.Fields, fields...)
	sbuild := &IngredientsSelect{IngredientsQuery: iq}
	sbuild.label = ingredients.Label
	sbuild.flds, sbuild.scan = &iq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a IngredientsSelect configured with the given aggregations.
func (iq *IngredientsQuery) Aggregate(fns ...AggregateFunc) *IngredientsSelect {
	return iq.Select().Aggregate(fns...)
}

func (iq *IngredientsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range iq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, iq); err != nil {
				return err
			}
		}
	}
	for _, f := range iq.ctx.Fields {
		if !ingredients.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if iq.path != nil {
		prev, err := iq.path(ctx)
		if err != nil {
			return err
		}
		iq.sql = prev
	}
	return nil
}

func (iq *IngredientsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Ingredients, error) {
	var (
		nodes       = []*Ingredients{}
		_spec       = iq.querySpec()
		loadedTypes = [1]bool{
			iq.withFood != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Ingredients).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Ingredients{config: iq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, iq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := iq.withFood; query != nil {
		if err := iq.loadFood(ctx, query, nodes,
			func(n *Ingredients) { n.Edges.Food = []*FoodIngredients{} },
			func(n *Ingredients, e *FoodIngredients) { n.Edges.Food = append(n.Edges.Food, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (iq *IngredientsQuery) loadFood(ctx context.Context, query *FoodIngredientsQuery, nodes []*Ingredients, init func(*Ingredients), assign func(*Ingredients, *FoodIngredients)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Ingredients)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.FoodIngredients(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(ingredients.FoodColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ingredients_food
		if fk == nil {
			return fmt.Errorf(`foreign-key "ingredients_food" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "ingredients_food" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (iq *IngredientsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := iq.querySpec()
	_spec.Node.Columns = iq.ctx.Fields
	if len(iq.ctx.Fields) > 0 {
		_spec.Unique = iq.ctx.Unique != nil && *iq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, iq.driver, _spec)
}

func (iq *IngredientsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(ingredients.Table, ingredients.Columns, sqlgraph.NewFieldSpec(ingredients.FieldID, field.TypeUUID))
	_spec.From = iq.sql
	if unique := iq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if iq.path != nil {
		_spec.Unique = true
	}
	if fields := iq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, ingredients.FieldID)
		for i := range fields {
			if fields[i] != ingredients.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := iq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := iq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := iq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := iq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (iq *IngredientsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(iq.driver.Dialect())
	t1 := builder.Table(ingredients.Table)
	columns := iq.ctx.Fields
	if len(columns) == 0 {
		columns = ingredients.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if iq.sql != nil {
		selector = iq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if iq.ctx.Unique != nil && *iq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range iq.predicates {
		p(selector)
	}
	for _, p := range iq.order {
		p(selector)
	}
	if offset := iq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := iq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// IngredientsGroupBy is the group-by builder for Ingredients entities.
type IngredientsGroupBy struct {
	selector
	build *IngredientsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (igb *IngredientsGroupBy) Aggregate(fns ...AggregateFunc) *IngredientsGroupBy {
	igb.fns = append(igb.fns, fns...)
	return igb
}

// Scan applies the selector query and scans the result into the given value.
func (igb *IngredientsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, igb.build.ctx, ent.OpQueryGroupBy)
	if err := igb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*IngredientsQuery, *IngredientsGroupBy](ctx, igb.build, igb, igb.build.inters, v)
}

func (igb *IngredientsGroupBy) sqlScan(ctx context.Context, root *IngredientsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(igb.fns))
	for _, fn := range igb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*igb.flds)+len(igb.fns))
		for _, f := range *igb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*igb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := igb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// IngredientsSelect is the builder for selecting fields of Ingredients entities.
type IngredientsSelect struct {
	*IngredientsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (is *IngredientsSelect) Aggregate(fns ...AggregateFunc) *IngredientsSelect {
	is.fns = append(is.fns, fns...)
	return is
}

// Scan applies the selector query and scans the result into the given value.
func (is *IngredientsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, is.ctx, ent.OpQuerySelect)
	if err := is.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*IngredientsQuery, *IngredientsSelect](ctx, is.IngredientsQuery, is, is.inters, v)
}

func (is *IngredientsSelect) sqlScan(ctx context.Context, root *IngredientsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(is.fns))
	for _, fn := range is.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*is.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := is.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
